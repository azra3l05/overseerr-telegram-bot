# Overseerr Telegram Bot — Python dependencies
# Install with:  pip install -r requirements.txt

python-telegram-bot>=20.0     # Telegram Bot API (v20+ async Application API)
requests>=2.28                # HTTP client for Overseerr / Radarr / Sonarr APIs
python-dotenv>=1.0            # Load configuration from a .env file

# Optional — PostgreSQL-backed availability checks & request logging
psycopg2-binary>=2.9

# Optional — web dashboard & health-check HTTP endpoints
flask>=2.2
